"""El grafo de contexto: extracción (`extraction.py`), traversal multi-hop
(`traversal.py`) y la glue de construcción (`build.py`) que responde P2 —
"si modifico la API de core-billing, ¿qué módulos se rompen?" — cuya
respuesta vive repartida entre los documentos de los servicios consumidores,
nunca en uno solo.
"""

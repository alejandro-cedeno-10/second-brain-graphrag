"""Adapters 100% offline: sin red, sin credenciales, sin AWS.

Es lo que hace posible ensayar y testear toda la demo en un laptop sin
conexión. FalkorDB es la única excepción con un proceso externo, y corre
en Docker en `localhost` — cero llamadas salen de la máquina.
"""
